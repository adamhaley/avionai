# Environment configuration for server-prod.py

# Language for OCR (en, ch, fr, de, etc.)
PADDLE_LANG=en

# Port to run the server on
PADDLE_PORT=6000

# Host to bind to
PADDLE_HOST=0.0.0.0

# CORS allowed origins (comma-separated)
# Use "*" for development, specific domains for production
PADDLE_ALLOWED_ORIGINS=*
